# @nekoglobal/networks

A complete crypto network base support for cross-chain

# Commands
  - Clean project
    ```bash
      npm run clean
    ```
  - build
    ```bash
      npm run build
    ```
  - publish npm (temporaral)
    ```bash
      npm run publish:network
    ```
# Ref
